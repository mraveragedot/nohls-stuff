// Shekinah code maker
// Nohl
// June 12

let code = "";

function setup() {
  createCanvas(windowWidth, windowHeight);
  code = prompt("type in your message to be decoded")
}

function draw() {
  background(0);
}
